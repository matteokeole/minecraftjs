const
	u = 1, // Block size
	fps = 60,
	// Generation
	amplitude = 8,
	inc = .05,
	// Chunks
	chunkSize = 10,
	// Distances
	renderDistance = 3,
	selectionDistance = 4.5,
	// Fog
	fogBlend = 1,
	fogDistance = 10,
	// Movement
	movingSpeed = .1125,
	acc = .115,
	// Player settings
	Settings = {
		autojump: false,
		borders: true
	};